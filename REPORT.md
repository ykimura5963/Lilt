# Lilt — プロジェクト分析レポート
*2026-06-14 時点 / アプリバージョン `LILT_VERSION = 1.13.2`*

## 全体構成
- **バックエンド**（`main.py` / FastAPI）：YouTube URL → `yt-dlp`（480p）+ `youtube-transcript-api`（字幕）→ **英文は字幕をそのまま使用**（`_simple_chunk_transcript` でルールベース段落化）→ **LLMは日本語訳のみ**（`_translate_window_safe`：ウィンドウ単位・解析失敗時は二分割→1文ずつのフォールバック。Ollama / RunPod / OpenRouter / OpenAI を OpenAI互換APIで統一する `_translate_chat`）→ **発音アノテーションはPythonのルールベース**（`_rule_annotate_para`、LLM不要・即時）→ `projects/{video_id}/{video.mp4, data.json, data.md, info.txt, notes.json}` に保存（保存時にテンポ時刻を焼き込み、schema `version:"2"`）。保存・一覧取得のたびに親フォルダへ **`video_index.json`**（バックエンド不要の静的プロジェクト一覧）を書き出し/更新する（`_scan_projects` / `_write_video_index`）。RunPod利用時は `/run`（即時job_id取得）→`/status`ポーリング→`/cancel` 方式で、タブを閉じる・キャンセル・Ctrl+C のいずれでも課金を停止する。

  **エンドポイント**：
  - `POST /process` — 生成パイプライン本体（SSEで進捗配信、ウィンドウ単位逐次保存、タイムアウト時フェイルファスト、保存後に `video_index.json` を更新）
  - `POST /retranslate/{video_id}` — 日本語訳のみ再生成（SSE進捗、プロバイダその場選択・RunPodコールドスタート対策のウィンドウ単位リトライ）
  - `GET /projects` — 保存済みプロジェクト一覧（任意の `root` 走査、呼び出し時に `video_index.json` も更新）
  - `GET /list-dirs` — PCの「動画を開く」フォルダブラウザ（ライブラリのルート選択）
  - `GET /library-file` — 任意ルート配下のファイル配信（トラバーサル対策・Range対応）
  - `POST /notes/{video_id}` — チャンクメモ（`notes.json`）の保存
  - `DELETE /projects/{video_id}` — プロジェクト削除（処理途中のものも削除可）
  - `GET /health` — ffmpeg/Ollama/モデル診断

  CORSは `ALLOWED_ORIGINS` で制限可、起動時に ffmpeg をプリフライトチェック。動画DL済みなら再DLをスキップして字幕取得から再開。

- **起動**：`start.bat` がワンクリックで ffmpeg 自動導入 → Ollama 起動（`OLLAMA_KEEP_ALIVE=30m`/`OLLAMA_NUM_PARALLEL=1` でCPU推論チューニング）→ バックエンド/フロント/ブラウザを自動起動。

- **フロントエンド**：`index.html`（マークアップ）＋ `styles.css` ＋ `js/01〜13`（関心ごとに分割した**順序付きクラシックスクリプト**。**ESモジュール（`type="module"`）は不採用** — 大量のインラインハンドラ（`onclick`/`oninput`）とトップレベル `let` への直接書き換えに依存しているため。複数スクリプトは単一のグローバル字句環境を共有し、挙動はモジュール化前のバイト等価コードと完全に等価）。

  **読み込み順序**（`index.html`）：`01-data → 02-state → 03-render-sync → 04-player → 05-sidebar → 06-generate-youtube → 07-generate-run → 08-annotate-tempo → 09-io-save-load → 10-shadowing → 12-notes → 13-local-player → 11-init`（初期化は最後）。

  | ファイル | 役割 |
  |---|---|
  | `01-data.js` | `PARAS` の初期サンプルデータ |
  | `02-state.js` | グローバル状態（表示レイヤー・速度・オフセット・メモ）と設定永続化（`SETTINGS_KEY`） |
  | `03-render-sync.js` | トランスクリプト描画・カラオケ式同期ハイライト |
  | `04-player.js` | ローカル/YouTube再生、シーク、速度、チャンクリピート |
  | `05-sidebar.js` | サイドバー（フレーズ一覧／凡例／タブ切替）、モバイルパネル |
  | `06-generate-youtube.js` | YouTube全自動生成UI、ライブラリ一覧 |
  | `07-generate-run.js` | 生成実行・SSE進捗処理 |
  | `08-annotate-tempo.js` | リズム同期エンジン（テンポモデルによる語タイミング配分） |
  | `09-io-save-load.js` | 保存/読込（FSA）、設定永続化、バックエンド健全性チェック |
  | `10-shadowing.js` | シャドーイング（録音・A/B・練習ループ） |
  | `12-notes.js` | チャンクメモ（`notes.json`） |
  | `13-local-player.js` | スマホ向けバックエンド不要ローカルプレイヤー |
  | `11-init.js` | 初期化（最後に読み込み） |

- **データ構造**：
  - `data.json`：`{ version:"2", contentBase, generatedAt, paras:[{ id, start, end, en, ja, words:[{ t, ws, wsUniform, stress, inton, elision, note, syl }] }] }` — `ws`=テンポ配分、`wsUniform`=均等割りを両方保存（オフライン互換）。
  - `notes.json`：`{ notes:{ "<paraIndex>": "本文" } }` — チャンク別自由メモ。`data.json`/`data.md` と独立しており再翻訳・再生成で消えない。
  - `video_index.json`（親フォルダ直下）：`{ version:"1", updatedAt, projects:[{ id, title, dir, data, video, md, notes }] }` — バックエンド不要のプロジェクト一覧。バックエンドが自動生成・更新。

- **モバイル**：`@media (max-width:640px)` によるレスポンシブ単一カラム。`js/13-local-player.js` による**完全バックエンド不要のローカルプレイヤー**を搭載（`<input webkitdirectory>` で親フォルダ選択 → `video_index.json` があれば最優先で一覧化、無ければ `data.json` を持つサブフォルダを走査）。再生コントロール（▶/⏸/スライダー/時刻）は動画下の専用バー `.play-bar` に固定。

---

## 1. 現在実装済みの機能
*（凡例：★ = v1.4.1以前から実装済み ／ 🆕 = v1.5.0〜v1.13.2（今期の一連のセッション）で追加・変更）*

### PC版（フル機能）

**再生・同期**
- ローカル動画/音声（`mp4`/`m4a`）**または** YouTube iframe 再生
- ★再生に連動した単語ごとのカラオケ式ハイライト（`timeupdate` / YTポーリング）
- ★再生速度 0.5×〜1.25×（**ピッチ保持** `preservesPitch`）、プログレスバーのクリックシーク、段落クリックでシーク
- ★同期オフセット調整（ドロップダウン、0.1秒刻み・±15.0秒）
- ★**チャンクリピート** — 任意の段落をループ再生（🔁 個別／全体トグル）
- ★**リズム同期（♪）** — テンポモデルによる語タイミング配分。均等割りとの切替可能
- ★**自動スクロール追従（📌）** トグル
- ★**文字サイズ調整（A＋）** — 1.0×⇔1.2×トグル
- 🆕 動画下のスライダーバーに**再生(▶)・停止(⏸)ボタン**を追加。動画のネイティブ操作UIは非表示にし、動画クリックで再生/一時停止をトグル（v1.9.0）

**アノテーション表示**
- ★強勢マーク ●（強）/ ◯（弱）
- ★抑揚カーブを SVG 表示（上昇 / 下降 / 上昇下降）
- ★消音・連結の × マークと IPA 弱化注記（ホバー表示）
- ★任意の音節分割表示（`syl`）
- ★レイヤー独立トグル（強弱 / 抑揚 / 消音 / リズム）
- ★英語・日本語の対訳表示

**シャドーイング練習（リプロダクション）※Tier 1 / Step 1–2 実装済み**
- ★**チャンク録音（🎤）** — 各チャンクヘッダから録音。カウントイン（3·2·1）→ 録音 → チャンク長で自動停止。録音中は 🎤 赤点滅＋**VUメーター**で入力レベル表示（v1.2.0）
- ★**A/B 比較** — 録音後に **▶モデル / ▶自分 / A·B（交互再生）**。モデルは区間シーク再生、自分の声はピッチ保持・再生速度連動で再生（v1.2.0）
- ★**練習ループ（🎧）** — `await` 状態機械で「聴く（×N）→ 間 → 自動録音 → 比較」をループ回数ぶん自動実行。練習ステータスバー（フェーズ／ループ）、⏸/⏭/⏹、▶▶連続練習、自己評価（★1–3）＋練習回数バッジ（`lilt.shadow.<base>` に保存＝Tier 3 SRS の種）。ループ回数／聴く回数／ポーズ長はコントロールバーで設定・永続化（v1.3.0）
- ★マイク許可エラーの明示、`MediaRecorder` 形式の自動フォールバック（webm/opus→webm→mp4）。録音は**ブラウザ内（メモリ）のみ**でサーバー送信なし
- **未実装**：波形オーバーレイ／メトロノーム／録音の永続化（Step 3–5、詳細は §3・§4）

**🆕 チャンクメモ（v1.10.0〜）**
- 各段落のボタン列とアノテーションの間にメモ欄を配置。未入力時は hover で「＋ メモ」トリガー表示、入力中はデバウンスで自動保存。保存先は動画フォルダの `notes.json`（`data.json`/`data.md` とは独立、再翻訳・再生成で消えない）。バックエンドは `POST /notes/{video_id}`
- 🆕 **メモのエクスポート/インポート**（v1.12.0）— 書き戻し不可な環境（スマホのローカルフォルダ読込など）向けに、`notes.json` 形式でファイルバーから書き出し/取込し、`localStorage` に保存・編集できる（PC↔スマホ間の橋渡し）

**生成・データ管理**
- ★**YouTube全自動**パイプライン（バックエンド連携、SSEでリアルタイム進捗バー）。英文はYouTube字幕をそのまま使用し、LLMは**日本語訳のみ**（発音アノテーションはバックエンドのルールベースで即時生成）
- ★**翻訳プロバイダの統合** — **Ollama / RunPod / OpenRouter / OpenAI** の4種をOpenAI互換で切替（接続テスト付き）。フロント（`resolveTranslateProvider`/`resolveProviderByName`）とバックエンド（`_translate_chat`）でパラメータ整合
- 🆕 **RunPodジョブの確実なキャンセル**（v1.5.0）— `/run`→`/status`ポーリング→`/cancel` 方式に変更し、タブを閉じる・キャンセルボタン・バックエンド終了（Ctrl+C）・SSE切断のいずれでも課金を停止。生成・再翻訳・アノテーション生成すべてに**キャンセルボタン**を追加（v1.6.0）
- 🆕 **動画ライブラリ**（v1.9.0）— ファイルバーの「▶ 動画を開く」がライブラリ一覧に。`GET /projects` で `projects/` または任意の親フォルダを一覧化、`title.txt`→`contentBase`→フォルダ名の順で表示名決定
- 🆕 **PCの「動画を開く」フォルダ選択UI**（v1.12.0）— `GET /list-dirs` によるフォルダブラウザで、ライブラリのルートをパス入力なしに選択可能（📁 フォルダ選択）
- 🆕 **バックエンド不要のプロジェクト一覧 `video_index.json`**（v1.13.0）— 親フォルダ直下の `video_index.json` があれば、バックエンド未起動でも「📂 フォルダから開く（BE不要）」ボタン→フォルダ選択だけで一覧・再生できる。バックエンドが生成完了時・`GET /projects` 時に常に最新化
- ★**保存済みプロジェクト一覧** — ワンクリック読込 ＋ 🗑 削除（処理途中のものも削除可）
- ★**バックエンド健全性バッジ**（`/health`：ffmpeg/Ollama/モデル）
- ★文字起こし入力：ペースト ／ **MD読込**（`data.md` をテキストエリアへ展開）
- ★**アノテーションのみ再生成（♺）** — 読込済みチャンクの発音記号だけを再ダウンロード・再翻訳なしで作り直し
- ★**日本語訳のみ再生成（🌐訳）** — 既存プロジェクトの英文を翻訳して `ja` を補完（`POST /retranslate/{id}`、SSE進捗、プロバイダを一覧上で都度選択可）
- ★動画名フォルダへの保存（`data.json` + `data.md` + 動画コピー）を File System Access API で実行、非対応時はダウンロードにフォールバック
- ★**メタ情報 `info.txt`** を動画と同フォルダに保存（Movie title / URL / Use LLM model / Use Lilt Version）
- ★**タイミング焼き込み**：保存JSONに `ws`＝テンポ配分＋`wsUniform`＝均等割りを両方記録（schema `version:"2"`、オフライン互換）
- ★既知フォルダの動画を開いた際に `data.json` + `data.md` を自動読込
- ★**設定の永続化**（`localStorage`）：表示レイヤー・リズム・追従・文字サイズ・速度・オフセット・統合プロバイダ設定（種別/モデル/エンドポイント/APIキー）を起動時復元
- ★JSON 入出力、トースト通知、進捗モーダル

**インフラ・保守性**
- ★`start.bat` ワンクリック起動（ffmpeg自動導入 → Ollama起動＋CPU推論チューニング → サーバー2種＋ブラウザ）
- ★コード分割：`index.html`（マークアップ）＋ `styles.css` ＋ 順序付きクラシックスクリプト。🆕 `js/10-shadowing.js`・`js/12-notes.js`・`js/13-local-player.js` を追加し**13ファイル構成**に
- ★`syncHighlight` の DOM要素キャッシュ化（再生ホットパス最適化）
- ★バックエンド：`/health`、`ALLOWED_ORIGINS`、ffmpegプリフライト、空字幕ガード
- 🆕 翻訳JSON解析の堅牢化（末尾カンマ・生の改行・CJK括弧での終端ミスなどを補修。v1.4.1/v1.7.1/v1.9.2）、Ollama既定モデル軽量化（`qwen3.5:2b`）と推論パラメータ縮小によるCPU負荷低減（v1.9.1）

### モバイル版（v1.11.0〜大幅刷新 — バックエンド不要のフル機能プレイヤー）
- 🆕 **ローカルプレイヤー**（v1.11.0）— 「動画を開く」が端末ローカルの親フォルダ選択（`<input webkitdirectory>`）になり、配下を一覧化。各ファイル（`video.*`/`data.json`/`notes.json`）を `File` から直接読み込み（バックエンド不要）
- 🆕 **`video_index.json` 優先読込**（v1.13.0）— 親フォルダに `video_index.json` があれば最優先で一覧化（順序・表示名・読込ファイル名を尊重）、無ければ `data.json` を持つサブフォルダを走査
- 🆕 **「動画を開く」の2段階化**（v1.12.0）— 押下時にいきなりOSのフォルダ選択ダイアログを開かず、まずライブラリのモーダル→「📁 親フォルダを選択」→選択後に動画一覧を表示
- 🆕 **UI刷新**（v1.11.0）— 動画を画面上部に固定し、各コントロールをアイコン化して動画上部に半透明オーバーレイ表示。ヘッダー高さ 44px→30px（PC含む）。生成系（YouTube全自動・再翻訳・設定タブ等）は引き続き非表示
- 🆕 **再生コントロールの固定**（v1.12.0/v1.13.1）— 再生/停止・プログレスバー・時刻表示を動画下の専用バー `.play-bar` に分離し、常に独立した行へ折り返す（`flex:1 1 100%`）
- 🆕 **本文表示の高密度化**（v1.12.1）— 英文 15px→13px（-15%）、日本語訳 12px→10px（-20%）に縮小し、行間も圧縮
- 🆕 **追従スクロールの行き過ぎ修正**（v1.12.1）— `scrollIntoView({block:'start'})` ＋ `.para` の `scroll-margin-top` で、現在行が固定動画の直下に収まるよう調整
- 🆕 **起動時プレースホルダーの削除**（v1.13.2）— 動画読込前は単に空の動画エリアを表示
- ★カラオケ同期・アノテーション表示はフル対応（強勢/抑揚/消音/リズム同期は既定ONで表示、レイヤー切替トグル自体は非表示）
- ★文字サイズ調整（A＋）・フレーズ一覧（タップでシーク）は利用可
- ★メモは読み取り専用表示＋`localStorage`への編集（エクスポート/インポートでPCの`notes.json`と橋渡し）
- 生成ツール・サイドバーの生成/設定タブ・FSAフォルダ保存は引き続きPC専用（シャドーイング録音もPC専用）

---

## 2. 改善できる点

> 進捗マーク（2026-06-14 更新）：✅ 完了 ／ 🟡 一部完了 ／ ⬜ 未着手

**文字起こし精度**
- ✅ **完了** 英文はYouTube字幕をそのまま使用（`_simple_chunk_transcript`）し、LLMは日本語訳のみ（同数・同順で翻訳、書き換え禁止を明示）。タイムスタンプも字幕由来となり、LLM出力のstart/endクランプ処理は不要となり削除済み（v1.1.0）。

**タイミング精度（最も効果が大きい）**
- ⬜ **未着手** 語レベルの `ws` は*推定値*（テンポモデル）で実測ではない。YouTubeはチャンク単位のタイムスタンプしか返さない。→ バックエンドに**強制アライメント**（`whisperx`/`aeneas`）を導入し、語ごとの実タイムスタンプを生成するのが理想（テンポモデルはフォールバックに格下げ）。
- ✅ **完了** 保存される `data.json` に `ws`＝テンポ配分（オフライン互換）＋ `wsUniform`＝均等割り を両方書き込み（schema `version:"2"`）。表示は `♪` トグルで切替可能（v1.0.0〜）。

**堅牢性・UX**
- ✅ **完了** `yt-dlp` のマージに必要な `ffmpeg` の欠落を、起動時プリフライト＋`GET /health`＋生成タブのバッジで明示。`start.bat` が ffmpeg を自動導入。
- ✅ **完了** ユーザー設定（表示レイヤー・リズム・追従・文字サイズ・速度・オフセット・プロバイダ設定）を `localStorage`（`SETTINGS_KEY`）に一元保存し起動時復元。
- 🟡 **一部完了** 処理ロジックの二重化：プロバイダ振り分けはフロント `resolveTranslateProvider`／バックエンド `_translate_chat` で整合済みだが、プロンプト本文そのものはまだフロント/バックエンドに別実装が残る（共通の真実源への一本化は未）。
- ✅ **完了** RunPod等が無応答時の実質ハングを解消（英語のみ先行保存＋ウィンドウ単位逐次保存・進捗送信・フェイルファスト、v1.1.0）。さらに **RunPodジョブが課金され続ける問題**を `/run`→`/status`→`/cancel` 方式で解消し、タブを閉じる・キャンセル・Ctrl+C・SSE切断のいずれでもジョブを停止（v1.5.0/v1.6.0）。
- ✅ **完了** 速度変更時のピッチ保持（`preservesPitch`）を動画読込・速度変更時に適用。シャドーイングのスロー再生で声の高さを維持。
- ✅ **完了** モバイルでバックエンドなしにプロジェクトを開く手段がなかった点 → `video_index.json` ＋ ローカルプレイヤー（`js/13-local-player.js`）で解消（v1.11.0/v1.13.0）。
- ✅ **完了** チャンクメモが再翻訳・再生成で消える/書き戻し不可環境で編集できない点 → `notes.json` 分離（v1.10.0）＋エクスポート/インポート（v1.12.0）で解消。
- ✅ **完了** PC版の再生コントロール（再生/停止/スライダー）がブラウザ幅によって1段目に混在する不具合 → `.play-bar` をflexコンテナ化し常に専用行へ折り返すよう修正（v1.13.1）。

**エンジニアリング**
- ✅ **完了** 2,200行の単一HTML → `index.html`（マークアップのみ）＋ `styles.css` ＋ `js/01〜13` のモジュール化で保守性向上。ESモジュール不採用（インラインハンドラ＋トップレベル`let`依存のため）、順序付きクラシックスクリプト方針を踏襲。
- ✅ **完了** `ALLOWED_ORIGINS` 環境変数でCORSを制限可能（既定は localhost 用に `*`）。
- 🟡 **一部完了** 自動テストは未導入（⬜）。`syncHighlight` はDOM要素キャッシュ（`EL`）導入済みで再生ホットパスは最適化済み。
- ✅ **完了** モバイル：フォントサイズ調整・追従（既定ON）・バックエンド不要のプロジェクト読込（`video_index.json`＋ローカルプレイヤー）をすべて実現。

> 各バージョンの詳細・対応コミットは [CHANGELOG.md](CHANGELOG.md)（v1.0.0〜v1.13.2）を参照。

---

## 3. リプロダクション（シャドーイング/スピーキング）支援ツールとしての将来ロードマップ

現状は**インプット/知覚**トレーナーとして強力（モデル音声を見る・聴く）に加え、**Tier 1 の中核ループ（録音・A/B・練習ループ）まで実装済み**。残るは波形・メトロノームによるフィードバック強化（Tier 1 後半）と、採点・定着（Tier 2/3）。

**Tier 1 — シャドーイングの中核ループ**
- ✅ **完了（v1.2.0）** チャンク単位の**マイク録音**（`MediaRecorder`）、即時再生、**A/B（モデル ↔ 自分の声）切替**
- ✅ **完了（v1.3.0）** **ピッチ保持スロー再生** ＋ ループ回数調整 ＋ 自動ポーズ発話（聴く → 間 → 録音 → 比較）、自己評価・練習統計バッジ
- ⬜ **未着手** **波形オーバーレイ**（モデル vs 学習者）でリズム・タイミングを視覚フィードバック — 既存のテンポエンジンと好相性
- ⬜ **未着手** リズム同期の重みから生成する**メトロノーム/ビート音**でプロソディ練習

**Tier 2 — フィードバック・採点**
- ⬜ 学習者録音への **ASR採点**（Whisper）→ 単語精度・脱落/挿入語をトランスクリプト上にハイライト
- ⬜ **抑揚カーブ比較** — 学習者のピッチを抽出し、既存の上昇/下降SVGカーブに重ねて表示
- ⬜ **語/音素レベルの発音採点**（Azure Pronunciation Assessment またはローカルモデル）を、既に描画している ● / × / IPA マークに対応付け

**Tier 3 — 練習モード・定着**
- ⬜ **クローズ/ディクテーションモード**（単語を隠し、要求時に表示）で能動的想起
- 🟡 **一部準備済み** **ブックマーク＆間隔反復（SRS）**キュー — チャンクごとの試行回数・自己評価は `lilt.shadow.<base>`（`localStorage`）に既に蓄積中。SRSスケジューリング自体は未実装
- ⬜ **Anki / CSV エクスポート**（音声 + 英文 + 和訳 + IPA）でオフライン復習
- 🟡 **一部実現** **オフライン PWA モバイルプレイヤー** — `video_index.json` ＋ローカルプレイヤーにより、焼き込み済みタイミングでのオフライン再生・アノテーション表示は実現済み。録音/シャドーイング機能はモバイル未対応

---

## 4. Tier 1 実装プラン — 残タスク（Step 3〜5）

> Step 1（録音+A/B、v1.2.0）・Step 2（練習ループ、v1.3.0）は実装済み（`js/10-shadowing.js`）。以下はその基盤の上に積む Step 3〜5 の計画。
> 実装時の想定バージョン：機能追加のため **MINOR**（例 `1.13.x → 1.14.0`）。出荷時に `LILT_VERSION`・`REPORT.md`冒頭・`CHANGELOG.md` を同期更新する。

### 統合ポイント（既存コードとの再利用）
- モデル再生区間・終端検知：`PARAS[pi].start..end` ＋ `jumpTo(annSec)`（`timeOffset`補正・local/YT両対応、既存）
- 速度・ピッチ：`applyPlaybackSpeed()` / `playSpeed`（`preservesPitch` 設定済み）
- 録音ストア：`_recStore = new Map()`（`paraId -> {blob, url, sec}`、既存）
- テンポ：`tempoWordStarts()` / `wordWeight()`（`08-annotate-tempo.js`、既存）
- 設定永続化：`SETTINGS_KEY` / `loadSettings()` / `saveSettings()`（`02-state.js`、既存）
- 統計：`lilt.shadow.<contentBase>`（`_loadStats`/`_saveStat`、既存）

### Step 3 — 波形オーバーレイ
- チャンク展開時に `<canvas class="wave">` を差し込み、モデル（ローカル/バックエンド音源）と学習者録音を `decodeAudioData` → 時間正規化して上下に重ね描画。
- **語頭オンセット線**（`tempoWordStarts`）を両波形に重畳し、**尺差（テンポ）readout**（例「あなたは 12% 遅い」＝録音長 ÷ モデル長）を表示。
- 波形クリックでその位置から部分再生（細部のリズム確認）。
- 制約：**YouTube iframe は音声バッファ取得不可** → モデル波形はローカル/バックエンド配信メディア限定。モデル波形は `vid` 全体を一度だけ `decodeAudioData` し、チャンク区間を切り出してキャッシュ。
- 受け入れ基準：ローカル/バックエンド動画で、モデルと自分の波形＋オンセット線が同一チャンク幅で重なって見え、尺差が数値で出る。

### Step 4 — メトロノーム / ビート
- `scheduleMetronome(paraId)`：`tempoWordStarts` の各オンセット時刻に `AudioContext.currentTime` 基準でクリックをスケジュール（強勢=強拍 / 弱化=弱拍）。`playSpeed` に追従。
- カウントイン小節＋音量（`metroVol`）設定をコントロールバーに **♩ メトロノーム** トグルとして追加（`SETTINGS_KEY` に永続化）。
- 受け入れ基準：チャンク再生に同期してプロソディのビートが鳴り、速度変更にも追従する。

### Step 5 — 永続化・エクスポート（任意）
- FSA フォルダ選択時、録音を `rec/<base>/<paraId>__<timestamp>.webm` に保存し、再読込時に復元。
- FSA非対応でも**単一テイクをダウンロード**（`<a download>`）で持ち出し可能に。
- 既存の練習統計（試行回数/自己評価/最終練習日）は既にチャンクヘッダにバッジ表示済み（`_statsBadge`）。Step 5ではAnki/CSVエクスポート（Tier 3）への布石として、統計のエクスポート機能を追加検討。

### ＋未着手：キーボードショートカット（ハンズフリー練習）
シャドーイングは口と耳が塞がるため、練習中のみ有効なショートカットを用意（入力欄フォーカス時は無効化）：
`Space`=モデル再生/停止・`R`=録音・`A`=自分の声・`B`=A/Bトグル・`N`/`P`=次/前チャンク・`Enter`=練習ループ開始/停止・`1/2/3`=自己評価。

### ＋未着手：連続シャドーイング・モード（将来オプション）
動画を通しで再生し、各チャンク終端で自動ポーズ→録音ウィンドウ→続行する「流し練習」。既存の終端検知（`checkChunkRepeat`系）を全チャンクへ一般化すれば実現可能。

### 変更ファイル（想定）
| ファイル | 変更 |
|---|---|
| `js/10-shadowing.js` | 波形描画・メトロノーム・ショートカット・録音永続化を追加 |
| `js/03-render-sync.js` | チャンクに `<canvas class="wave">` を追加 |
| `js/02-state.js` | `metronome`/`metroVol` 等の設定項目を追加 |
| `js/09-io-save-load.js` | FSAへの録音保存/復元 |
| `index.html` | コントロールバーに ♩ メトロノーム、隠し要素の追加 |
| `styles.css` | 波形キャンバス・メトロノームトグルのスタイル |

### 検証
- Preview（`python -m http.server` + ブラウザ）で：波形描画・尺差readout、メトロノーム同期、ショートカット、設定/統計の永続化を確認。
- マイクはヘッドレス自動化しづらいため、`MediaRecorder` をモックして状態機械・タイマー遷移を検証し、実マイクは手動確認。
- 既存機能（カラオケ同期・チャンクリピート・速度/オフセット・既存のシャドーイングStep1-2）の非回帰を確認。
